# RequestSessionRecovery Mutation
# Sends a magic link email to the parent for session recovery
# Requires parent email to be collected first

mutation RequestSessionRecovery($input: RequestRecoveryInput!) {
  requestSessionRecovery(input: $input) {
    success
    message
  }
}
