# SubmitAssessmentMessage Mutation
# Sends a message to the AI assessment chatbot and gets a response

mutation SubmitAssessmentMessage($input: SubmitAssessmentMessageInput!) {
  submitAssessmentMessage(input: $input) {
    id
    summary {
      keyConcerns
      childName
      recommendedFocus
      generatedAt
    }
    isFitForDaybreak
    suggestedNextSteps
    isComplete
    conversationHistory {
      id
      sender
      content
      timestamp
    }
  }
}
