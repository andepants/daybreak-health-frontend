# AbandonSession Mutation
# Abandons an onboarding session explicitly
# Session data is retained per data retention policy
# Idempotent: abandoning already abandoned session succeeds

mutation AbandonSession($input: AbandonSessionInput!) {
  abandonSession(input: $input) {
    session {
      id
      status
    }
    success
  }
}
