# Production Environment Configuration
# Next.js automatically loads this file during production builds (npm run build)
# These values get inlined into the JavaScript bundle at build time

# ========================================
# Support & Communication (Intercom)
# ========================================

# Intercom App ID for live chat support widget
# This is safe to expose - it's a public identifier (like a website ID)
# Get from: Intercom Settings > Installation > Web
NEXT_PUBLIC_INTERCOM_APP_ID=lcek1iz4
